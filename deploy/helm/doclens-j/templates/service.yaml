apiVersion: v1
kind: Service
metadata:
  name: {{ include "doclens-j.fullname" . }}
  labels:
    {{- include "doclens-j.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "doclens-j.selectorLabels" . | nindent 4 }}
