## 1. Theme Coverage

- [x] 1.1 Add a theme token module with the orange palette and a unit test that locks the primary, hover, pressed, supplementary, and chart accent colors.
- [x] 1.2 Wire the theme token module into `src/App.vue` and update `src/assets/main.css` to the matching orange CSS variables.

## 2. Accent Sweep

- [x] 2.1 Update the chart palette and any hardcoded blue accents in `LatencyChart.vue`, `UploadFilePicker.vue`, `ProcessingRail.vue`, and `OcrModelList.vue`.
- [x] 2.2 Verify the dashboard still builds and the focused tests pass after the palette change.
