## 1. Dashboard Detail API

- [x] 1.1 Add a focused backend test and extend the Dashboard batch row read model so batch detail exposes `callback_url`, `idempotency_key`, and `metadata`.

## 2. Dashboard Detail UI

- [ ] 2.1 Add a focused Vue component test and batch intake information panel for callback URL, idempotency key, formatted metadata, and empty placeholders.

## 3. Verification And Archive

- [ ] 3.1 Run focused backend/frontend tests plus broader verification for touched modules, then archive the OpenSpec change.
