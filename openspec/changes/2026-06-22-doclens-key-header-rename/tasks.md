## 1. Rename caller partition header

- [ ] 1.1 Add failing frontend and backend tests proving `X-Doclens-Key` is the only caller partition header and `X-Recall-Key` is ignored.
- [ ] 1.2 Update Dashboard, backend request mapping, documentation, and generated runtime artifacts as needed to use `X-Doclens-Key`.
- [ ] 1.3 Run focused frontend/backend verification plus OpenSpec and whitespace checks.
