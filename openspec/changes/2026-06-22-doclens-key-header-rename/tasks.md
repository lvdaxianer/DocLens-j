## 1. Rename caller partition header

- [x] 1.1 Rename the caller partition header to `X-Doclens-Key` across tests, Dashboard forwarding, backend request resolution, documentation, generated runtime artifacts, and verification.
