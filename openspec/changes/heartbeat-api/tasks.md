## 1. Contract Definition

- [ ] 1.1 Add a failing contract test for `GET /api/v1/heartbeat` that
  asserts the response status and payload shape.
- [ ] 1.2 Verify the existing `/api/v1/health` contract remains
  unchanged.

## 2. Endpoint Delivery

- [ ] 2.1 Implement the heartbeat endpoint in the backend controller
  layer with a lightweight payload.
- [ ] 2.2 Run focused and broader backend verification, then validate
  the OpenSpec change.
