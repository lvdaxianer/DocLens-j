## 1. Docker runtime artifact contract

- [x] 1.1 Add a focused Dockerfile contract verification script and update
  Dockerfile plus entrypoint to satisfy the Ubuntu/local runtime artifact
  contract.
- [ ] 1.2 Update runtime artifact preparation script and packaging docs for
  local JDK21, Node22, and PostgreSQL build inputs.

## 2. Verification and archive

- [ ] 2.1 Run focused and broader verification for the local-runtime Docker
  packaging change.
